<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ZODIAKS.AppShel"
             xmlns:views="clr-namespace:ZODIAKS.Views">
    <TabBar>
        <ShellContent
            Title = "Zodiaks"
            Icon = "icon_feed.png"
            ContentTemplate = "{DataTemplate views:ZodiaksPage}" >
        </ShellContent>

        <ShellContent
            Title = "About"
            Icon = "icon_about.png"
            ContentTemplate = "{DataTemplate views:AboutPage}" >
        </ShellContent>
    </TabBar>
</Shell>